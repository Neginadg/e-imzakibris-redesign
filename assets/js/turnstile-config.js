
// Cloudflare Turnstile site key — public by design, safe to expose (like the
// Supabase publishable key in supabase-config.js). Get this from your
// Cloudflare dashboard: Turnstile > Add site. Replace the placeholder below,
// then set the matching TURNSTILE_SECRET_KEY as a server-side env var
// (Vercel project settings) — the secret key must never appear here.
window.EIMZA_TURNSTILE_CONFIG = {
  siteKey: '0x4AAAAAAEC1cBcLsUCjk-Lb'
};
